import type { HelixExtensionTransactionData, HelixResponse } from 'twitch';
import { HelixExtensionTransaction } from 'twitch';
import type { WebHookListener } from '../WebHookListener';
import { Subscription } from './Subscription';
/**
 * @private
 */
export declare class ExtensionTransactionSubscription extends Subscription<HelixExtensionTransaction> {
    private readonly _extensionId;
    constructor(_extensionId: string, handler: (data: HelixExtensionTransaction) => void, client: WebHookListener, validityInSeconds?: number);
    get id(): string;
    protected transformData(response: HelixResponse<HelixExtensionTransactionData>): HelixExtensionTransaction;
    protected _subscribe(): Promise<void>;
    protected _unsubscribe(): Promise<void>;
}
