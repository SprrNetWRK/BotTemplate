import type { HelixResponse, HelixStreamData } from 'twitch';
import { HelixStream } from 'twitch';
import type { WebHookListener } from '../WebHookListener';
import { Subscription } from './Subscription';
/**
 * @private
 */
export declare class StreamChangeSubscription extends Subscription<HelixStream | undefined> {
    private readonly _userId;
    constructor(_userId: string, handler: (data: HelixStream) => void, client: WebHookListener, validityInSeconds?: number);
    get id(): string;
    protected transformData(response: HelixResponse<HelixStreamData>): HelixStream | undefined;
    protected _subscribe(): Promise<void>;
    protected _unsubscribe(): Promise<void>;
}
