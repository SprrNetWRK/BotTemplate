import type { HelixResponse, HelixUserData } from 'twitch';
import { HelixUser } from 'twitch';
import type { WebHookListener } from '../WebHookListener';
import { Subscription } from './Subscription';
/**
 * @private
 */
export declare class UserChangeSubscription extends Subscription<HelixUser> {
    private readonly _userId;
    private readonly _withEmail;
    constructor(_userId: string, handler: (data: HelixUser) => void, _withEmail: boolean, client: WebHookListener, validityInSeconds?: number);
    get id(): string;
    protected transformData(response: HelixResponse<HelixUserData>): HelixUser;
    protected _subscribe(): Promise<void>;
    protected _unsubscribe(): Promise<void>;
}
