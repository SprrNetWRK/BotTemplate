import type { HelixFollowData, HelixResponse } from 'twitch';
import { HelixFollow } from 'twitch';
import type { WebHookListener } from '../WebHookListener';
import { Subscription } from './Subscription';
/**
 * @private
 */
export declare class FollowsFromUserSubscription extends Subscription<HelixFollow> {
    private readonly _userId;
    constructor(_userId: string, handler: (data: HelixFollow) => void, client: WebHookListener, validityInSeconds?: number);
    get id(): string;
    protected transformData(response: HelixResponse<HelixFollowData>): HelixFollow;
    protected _subscribe(): Promise<void>;
    protected _unsubscribe(): Promise<void>;
}
